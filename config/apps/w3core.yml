app_id: w3core
name: W3 Core

repo:
  url: ""
  default_dev_branch: dev/v0.5.40
  allowed_branch_pattern: '^dev/v[0-9]+\.[0-9]+\.[0-9]+$'
  blocked_branches:
    - main
    - master

paths:
  deploy: /opt/w3core-deploy
  runtime: /opt/w3core
  workspaces: /opt/w3forge-workspaces/w3-core
  scripts: /opt/scripts
  logs: /opt/logs/w3core
  backups: /opt/backups/w3core

service:
  name: w3core

authority:
  may_inspect: true
  may_edit_dev_branch: true
  may_commit_dev_branch: true
  may_push_dev_branch: true
  may_deploy: false
  may_tag_release: false
  may_modify_production_data: false

# v0.2.0 workflow control: optional validation/test commands.
# Commands run inside paths.workspaces via w3-app-test.sh.
# Deploy / release / package-movement commands are blocked by guard.
#
# v0.2.1: no W3 Core self-tests configured yet. Workflow status may
# remain WARN until safe commands are added. require_local_model is
# opt-in; when false or absent the workflow does not WARN if Ollama
# is missing.
tests:
  validate_config: true
  require_local_model: false
  commands: []
